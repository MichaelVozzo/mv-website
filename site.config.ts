type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Michael Vozzo",
  site_description: "Michael Vozzo's blog and portfolio",
  site_domain: "https://michaelvozzo.com",
};
